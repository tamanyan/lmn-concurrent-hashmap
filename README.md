LmnConcurrentMap
===========

C library of concurrent maps (hash tables, trees) with high performance for parallel model checking

## How to build
    
    $ autoreconf -i
    $ ./configure
    $ make
